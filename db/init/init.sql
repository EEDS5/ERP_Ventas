-- init.sql
-- Este script se ejecuta al iniciar el contenedor con un volumen vacío.

-- Si necesitas forzar la recreación (aunque normalmente no se recomienda en producción):
-- DROP DATABASE IF EXISTS ERP_Ventas;

CREATE DATABASE ERP_Ventas;